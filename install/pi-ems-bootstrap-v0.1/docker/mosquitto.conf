persistence true
persistence_location /mosquitto/data/
listener 1883
allow_anonymous true
# Bootstrap only: broker has no host port and is reachable only on ems-net.
# Add authentication before any future LAN exposure.
