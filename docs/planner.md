# Planner

<div id="planner-list"></div>
<div id="pm-tooltip" class="pm-tooltip" hidden></div>

<style>
.planner-minimal{--g:64px;max-width:1200px;margin:0 auto 2.2rem}.pm-status{padding:1rem 0;color:var(--md-default-fg-color--light)}.pm-status.pm-error{color:var(--md-typeset-color)}.pm-section{margin:1.1rem 0 1.5rem}.pm-section h2{font-size:.9rem;margin:0 0 .3rem}.pm-note{margin:.15rem 0 .45rem;color:var(--md-default-fg-color--light);font-size:.7rem}.pm-legend{display:flex;gap:.7rem;margin-bottom:.4rem;flex-wrap:wrap}.pm-legend span{font-size:.64rem;display:inline-flex;align-items:center;gap:.25rem}.pm-key{width:.62rem;height:.62rem;border-radius:2px;display:inline-block}.pm-key.base,.pm-bar.base{background:#6e7f90}.pm-key.pv,.pm-bar.pv{background:#d7a900}.pm-key.net,.pm-bar.net.import{background:#3277b3}.pm-bar.net.export{background:#2e9b67}.pm-row,.pm-aligned-row{display:grid;grid-template-columns:var(--g) minmax(0,1fr);gap:.4rem}.pm-quarter-grid{display:grid;grid-template-columns:repeat(96,minmax(0,1fr));gap:1px}.pm-forecast-frame{height:165px;border:1px solid var(--md-default-fg-color--lightest);border-radius:.45rem;overflow:hidden}.pm-balance-grid{height:100%;padding:.35rem .2rem .2rem}.pm-balance-col{position:relative;height:100%;cursor:crosshair}.pm-balance-col:hover{background:color-mix(in srgb,var(--md-default-fg-color) 6%,transparent)}.pm-zero{position:absolute;left:0;right:0;top:50%;border-top:1px solid var(--md-default-fg-color--lightest);pointer-events:none}.pm-bar{position:absolute;width:26%;min-height:2px;pointer-events:none}.pm-bar.base{left:4%}.pm-bar.pv{left:36%}.pm-bar.net{left:68%}.pm-axis-row{margin-top:.25rem}.pm-time-axis{padding:0 .2rem;min-height:14px}.pm-time{font-size:.48rem;opacity:.7;white-space:nowrap}.pm-meta,.pm-deadline{margin:.15rem 0 .45rem calc(var(--g) + .4rem);font-size:.65rem}.pm-deadline{padding:.38rem .55rem;border:1px solid var(--md-default-fg-color--lightest);border-radius:.35rem;font-weight:600}.pm-slots{display:grid;gap:.22rem}.pm-row{align-items:center}.pm-row-label{font-size:.62rem;font-weight:700;opacity:.78;text-align:right}.pm-track-frame{height:24px;border-bottom:1px solid var(--md-default-fg-color--lightest)}.pm-track{height:100%;padding:0 .2rem;align-items:center}.pm-segment{height:18px;border-radius:.25rem;align-self:center}.pm-segment.tesla{background:#8f4ac7}.pm-segment.boiler{background:#2aa9b8}.pm-segment.battery{background:#66aa45}.pm-empty{grid-column:1/-1;font-size:.62rem;opacity:.65;line-height:24px}.pm-tooltip{position:fixed;z-index:9999;min-width:220px;max-width:320px;padding:.55rem .65rem;border:1px solid var(--md-default-fg-color--lightest);border-radius:.4rem;background:var(--md-default-bg-color);box-shadow:0 6px 24px rgba(0,0,0,.18);font-size:.62rem;line-height:1.45;pointer-events:none}.pm-tooltip strong{display:block;margin-bottom:.25rem;font-size:.66rem}.pm-tooltip-row{display:flex;justify-content:space-between;gap:1rem}.pm-tooltip-row span:first-child{opacity:.72}.pm-tooltip-reason{margin-top:.3rem;padding-top:.3rem;border-top:1px solid var(--md-default-fg-color--lightest);opacity:.82;word-break:break-word}@media(max-width:600px){.planner-minimal{--g:54px}.pm-forecast-frame{height:150px}.pm-tooltip{display:none!important}}
</style>

<script>
(() => {
const defs=[
['Homey Planner','https://raw.githubusercontent.com/OnsKasteeltje/homey-energy-manual/main/docs/data/energy-planner-shadow.json'],
['Pi Planner (shadow)','https://raw.githubusercontent.com/OnsKasteeltje/homey-energy-manual/main/docs/data/energy-planner-shadow-pi.json'],
['Dynamic Pi Planner (shadow)','https://raw.githubusercontent.com/OnsKasteeltje/homey-energy-manual/main/docs/data/energy-planner-shadow-dynamic.json']];
const finite=v=>v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v));
const unwrap=x=>x?.plan?.plan?.actions?x.plan:(x?.plan||x||{});
const hhmm=x=>{const d=new Date(x);return Number.isNaN(d.getTime())?'—':d.toLocaleTimeString('nl-NL',{hour:'2-digit',minute:'2-digit'})};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmtW=v=>finite(v)?`${Math.round(Number(v))} W`:'—';
const tooltip=document.getElementById('pm-tooltip');
const list=document.getElementById('planner-list');
const roots=defs.map(([title,url])=>{const r=document.createElement('div');r.className='planner-minimal';r.dataset.url=url;r.innerHTML=`<h2>${title}</h2><div class="pm-status">Plannerdata laden…</div><div class="pm-content" hidden><section class="pm-section"><h2>24-uurs forecast</h2><p class="pm-note">Base load, PV en verwachte import/export. Positief = import; negatief = export.</p><div class="pm-legend"><span><i class="pm-key base"></i>Base load</span><span><i class="pm-key pv"></i>PV</span><span><i class="pm-key net"></i>Verwachte import/export</span></div><div class="pm-aligned-row"><div></div><div class="pm-forecast-frame"><div class="pm-quarter-grid pm-balance-grid pm-forecast"></div></div></div><div class="pm-aligned-row pm-axis-row"><div></div><div class="pm-quarter-grid pm-time-axis"></div></div></section><section class="pm-section"><h2>Geplande tijdsvakken</h2><div class="pm-meta" hidden></div><div class="pm-deadline" hidden></div><div class="pm-slots"></div></section></div>`;list.append(r);return r;});
async function get(root){const q=await fetch(`${root.dataset.url}?ts=${Date.now()}`,{cache:'no-store'});if(!q.ok)throw new Error(`HTTP ${q.status}`);const payload=await q.json(),p=unwrap(payload),actions=Array.isArray(p?.plan?.actions)?p.plan.actions:[];if(actions.length!==96)throw new Error(`verwacht 96 kwartiersslots, ontvangen ${actions.length}`);return{p,actions};}
Promise.allSettled(roots.map(get)).then(results=>{results.forEach((res,i)=>{if(res.status==='fulfilled'){const axisStart=new Date(res.value.actions[0].start).getTime();render(roots[i],res.value,axisStart,96);}else{const s=roots[i].querySelector('.pm-status');s.className='pm-status pm-error';s.textContent=i===2?'Wacht op eerste Dynamic Planner snapshot van de Pi.':`Plannerdata laden mislukt: ${res.reason.message}`;}});});
function slotTooltip(a){
 const rows=[];
 const add=(k,v)=>{if(v!==null&&v!==undefined&&v!=='')rows.push(`<div class="pm-tooltip-row"><span>${esc(k)}</span><b>${esc(v)}</b></div>`)};
 add('Base load',fmtW(a.baseLoadForecastW));
 add('PV',fmtW(a.pvForecastW));
 add('Import/export vóór flex',fmtW(a.netBeforeFlexW));
 if(finite(a.correctedExportBeforeFlexW))add('Gecorrigeerde export',fmtW(a.correctedExportBeforeFlexW));
 if(finite(a.confidence))add('Forecast confidence',`${Math.round(Number(a.confidence)*100)}%`);
 const ev=finite(a?.targets?.evTargetW)?Number(a.targets.evTargetW):0;
 const ww=finite(a?.targets?.wwTargetW)?Number(a.targets.wwTargetW):0;
 if(ev>0||String(a.tesla||'').toUpperCase()==='RUN')add('Tesla',`${String(a.tesla||'RUN').toUpperCase()} · ${fmtW(ev)}`);
 else add('Tesla',String(a.tesla||'HOLD').toUpperCase());
 if(ww>0||String(a.warmWater||'').toUpperCase()==='RUN')add('Boiler',`${String(a.warmWater||'RUN').toUpperCase()} · ${fmtW(ww)}`);
 else add('Boiler',String(a.warmWater||'HOLD').toUpperCase());
 if(finite(a.netAfterFlexW))add('Netpositie ná flex',fmtW(a.netAfterFlexW));
 else if(finite(a.gridImportAfterFlexW)||finite(a.gridExportAfterFlexW)){const imp=finite(a.gridImportAfterFlexW)?Number(a.gridImportAfterFlexW):0,exp=finite(a.gridExportAfterFlexW)?Number(a.gridExportAfterFlexW):0;add('Na flex',imp>0?`import ${Math.round(imp)} W`:exp>0?`export ${Math.round(exp)} W`:'0 W');}
 const reasons=[a.evAllocationReason,a.wwAllocationReason,a?.teslaPlan?.reason,a?.warmWaterPlan?.reason].filter(x=>x&&String(x).toUpperCase()!=='HOLD');
 const reason=[...new Set(reasons.map(String))].join(' · ');
 return `<strong>${esc(hhmm(a.start))}–${esc(hhmm(a.end))}</strong>${rows.join('')}${reason?`<div class="pm-tooltip-reason">${esc(reason)}</div>`:''}`;
}
function bindSlotTooltip(el,a){if(!a)return;el.addEventListener('mouseenter',e=>{tooltip.innerHTML=slotTooltip(a);tooltip.hidden=false;moveTooltip(e)});el.addEventListener('mousemove',moveTooltip);el.addEventListener('mouseleave',()=>{tooltip.hidden=true});}
function moveTooltip(e){if(tooltip.hidden)return;const pad=14,w=tooltip.offsetWidth||260,h=tooltip.offsetHeight||180;let x=e.clientX+14,y=e.clientY+14;if(x+w+pad>window.innerWidth)x=e.clientX-w-14;if(y+h+pad>window.innerHeight)y=e.clientY-h-14;tooltip.style.left=`${Math.max(pad,x)}px`;tooltip.style.top=`${Math.max(pad,y)}px`;}
function render(root,{p,actions},axisStart,axisSlots){const status=root.querySelector('.pm-status'),content=root.querySelector('.pm-content'),forecast=root.querySelector('.pm-forecast'),timeAxis=root.querySelector('.pm-time-axis'),slots=root.querySelector('.pm-slots'),meta=root.querySelector('.pm-meta'),deadline=root.querySelector('.pm-deadline');const t=p?.inputs?.tesla||{};if(t.deadlineActive===true){deadline.textContent=`Tesla deadline actief · ${hhmm(t.deadlineAt)}`;deadline.hidden=false;}if(p?.controlMode==='PURE_SHADOW'||p?.confidenceModel){const a=['PURE SHADOW'];if(finite(p?.realtime?.actualP1ExportW))a.push(`P1 export ${Math.round(Number(p.realtime.actualP1ExportW))} W`);if(finite(p?.realtime?.recentLocalAccuracy))a.push(`forecast accuracy ${Math.round(Number(p.realtime.recentLocalAccuracy)*100)}%`);meta.textContent=a.join(' · ');meta.hidden=false;}const vals=[];actions.forEach(a=>['baseLoadForecastW','pvForecastW','netBeforeFlexW'].forEach(k=>{if(finite(a[k]))vals.push(Math.abs(Number(a[k])))}));const max=Math.max(1000,...vals),by=new Map(actions.map(a=>[new Date(a.start).getTime(),a]));forecast.style.gridTemplateColumns=`repeat(${axisSlots},minmax(0,1fr))`;timeAxis.style.gridTemplateColumns=`repeat(${axisSlots},minmax(0,1fr))`;for(let i=0;i<axisSlots;i++){const ms=axisStart+i*900000,a=by.get(ms),c=document.createElement('div');c.className='pm-balance-col';c.style.gridColumn=`${i+1}`;const z=document.createElement('div');z.className='pm-zero';c.append(z);if(a){bindSlotTooltip(c,a);[['base',a.baseLoadForecastW],['pv',a.pvForecastW],['net',a.netBeforeFlexW]].forEach(([k,v])=>{if(!finite(v))return;const n=Number(v),h=Math.max(2,Math.min(48,Math.abs(n)/max*48)),b=document.createElement('div');b.className=`pm-bar ${k}`;b.style.height=`${h}%`;if(k==='net'){b.classList.add(n<0?'export':'import');b.style.bottom=n<0?`${50-h}%`:'50%'}else b.style.bottom='50%';c.append(b)});}forecast.append(c);if(i%12===0){const x=document.createElement('span');x.className='pm-time';x.style.gridColumn=`${i+1} / span 12`;x.textContent=hhmm(new Date(ms).toISOString());timeAxis.append(x);}}const active=(asset,a)=>!['HOLD','NONE','OFF','0',''].includes(String(a?.[asset]??'HOLD').toUpperCase());const row=(label,asset,css)=>{const r=document.createElement('div');r.className='pm-row';const n=document.createElement('div');n.className='pm-row-label';n.textContent=label;const f=document.createElement('div');f.className='pm-track-frame';const tr=document.createElement('div');tr.className='pm-quarter-grid pm-track';f.append(tr);r.append(n,f);let start=null;const flush=end=>{if(start===null)return;const s=document.createElement('div');s.className=`pm-segment ${css}`;s.style.gridColumn=`${start+1} / ${end+1}`;const first=actions[start],last=actions[end-1];let extra='';if(asset==='tesla'){const w=actions.slice(start,end).map(x=>x?.targets?.evTargetW).filter(finite).map(Number);if(w.length)extra=` · ${Math.round(Math.min(...w))}–${Math.round(Math.max(...w))} W`;}if(asset==='warmWater'){const w=actions.slice(start,end).map(x=>x?.targets?.wwTargetW).filter(finite).map(Number);if(w.length)extra=` · ${Math.round(Math.min(...w))}–${Math.round(Math.max(...w))} W`;}s.title=`${label}: ${hhmm(first.start)}–${hhmm(last.end)}${extra}`;tr.append(s)};for(let i=0;i<=actions.length;i++){const on=i<actions.length&&active(asset,actions[i]);if(on&&start===null)start=i;if(!on&&start!==null){flush(i);start=null}}if(!tr.children.length){const e=document.createElement('div');e.className='pm-empty';e.textContent='geen tijdsvakken';tr.append(e)}slots.append(r)};row('Tesla','tesla','tesla');row('Boiler','warmWater','boiler');if(actions.some(a=>active('battery',a)))row('Accu','battery','battery');status.remove();content.hidden=false;}
})();
</script>
