(function(){
  // Homepage architecture v1.8: deadlinekaart sluit verlopen Tesla-deadlines af op basis van runtime-resultaat.
  const DATA_BASE='/homey-energy-manual/data/';
  const script=document.createElement('script');
  script.src='/homey-energy-manual/javascripts/home-architecture-v1.js';
  let commandCache=null,statusCache=null,phaseCache=null,runtimeCache=null,enhanceTimer=null;
  async function getJson(name){const r=await fetch(`${DATA_BASE}${name}?ts=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`${name}: ${r.status}`);return r.json();}
  function fmtDeadline(v){if(!v)return '—';const d=new Date(String(v).replace(' ','T'));return Number.isNaN(d.getTime())?String(v):d.toLocaleString('nl-NL',{weekday:'short',day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'});}
  function runtimeLabel(s){const map={GEEN_DEADLINE:'Opportunistisch',WACHT_OP_PV:'Wacht op PV',OPPORTUNISTISCH:'Opportunistisch laden',DEADLINE_WACHT:'Wacht op geschikt laadmoment',DEADLINE_OPPORTUNISTISCH:'Opportunistisch laden',DEADLINE_CATCH_UP:'Catch-up actief',DEADLINE_GEMIST_CATCH_UP:'Deadline gepasseerd · catch-up',DEADLINE_PRIJS_NEGATIEF:'Laden · negatieve stroomprijs',DEADLINE_PV_OVERSCHOT:'Laden op PV-overschot',DEADLINE_WACHT_PV_FORECAST:'Wacht op voorspelde PV',DEADLINE_PRIJS_GOEDKOOP:'Laden · gunstige stroomprijs',DEADLINE_WACHT_GOEDKOPER:'Wacht op goedkoper laadmoment',DOEL_GEHAALD:'Laaddoel gehaald',NIET_AANGESLOTEN:'Tesla niet aangesloten',CONFIG_FOUT:'Deadlineconfiguratie fout',BASELINE_FOUT:'Meetbaseline ontbreekt',KALIBRATIE_AFWIJKING:'Kalibratie/meetafwijking'};return map[String(s||'').toUpperCase()]||'';}
  function deadlineDate(v){if(!v)return null;const d=new Date(String(v).replace(' ','T'));return Number.isNaN(d.getTime())?null:d;}
  function expiredResult(command,runtime){
    const deadline=deadlineDate(runtime?.deadline||command?.deadline);if(!deadline||deadline.getTime()>Date.now())return null;
    const status=String(runtime?.status||runtime?.decision?.status||'').toUpperCase();
    const audit=runtime?.audit||runtime?.decision?.audit||{};
    const remaining=Number(runtime?.remainingKWh);
    const uncertain=['KALIBRATIE_AFWIJKING','BASELINE_FOUT','CONFIG_FOUT'].includes(status)||audit.anomaly===true||audit.baselineValid===false;
    if(status==='DOEL_GEHAALD'||(Number.isFinite(remaining)&&remaining<=0&&!uncertain))return {kind:'reached',headline:'Tesla gereed',state:'Doel voor deadline gehaald',sub:`Deadline was ${fmtDeadline(deadline.toISOString())}`};
    if(uncertain)return {kind:'unknown',headline:'Tesla',state:'Deadline verstreken · resultaat onzeker',sub:`Deadline was ${fmtDeadline(deadline.toISOString())} · ${runtimeLabel(status)||'meetresultaat niet betrouwbaar'}`};
    if(Number.isFinite(remaining)&&remaining>0)return {kind:'missed',headline:'Tesla deadline gemist',state:'Doel niet vóór deadline gehaald',sub:`${remaining.toFixed(1)} kWh resterend · deadline was ${fmtDeadline(deadline.toISOString())}`};
    return {kind:'unknown',headline:'Tesla',state:'Deadline verstreken · resultaat onbekend',sub:`Deadline was ${fmtDeadline(deadline.toISOString())}`};
  }
  async function refreshData(){const [status,command,phase,runtime]=await Promise.all([getJson('homey-status.json').catch(()=>statusCache),getJson('tesla-deadline-command.json').catch(()=>commandCache),getJson('pv-phase-24h.json').catch(()=>phaseCache),getJson('tesla-runtime.json').catch(()=>runtimeCache)]);if(status)statusCache=status;if(command)commandCache=command;if(phase)phaseCache=phase;if(runtime)runtimeCache=runtime;}
  function applyOverlay(){
    const status=statusCache||{},command=commandCache||{},phase=phaseCache||{},runtimeData=runtimeCache||{};
    document.querySelectorAll('.ha-goal').forEach(goal=>{const title=goal.querySelector('strong'),state=goal.querySelector('.ha-goal-state'),sub=goal.querySelector('small');if(title?.textContent.trim()==='Warm water'&&state?.textContent.trim()==='Doel vandaag gehaald'){state.textContent='Boiler op temperatuur';if(sub)sub.textContent='Warmwaterdoel voor vandaag bereikt';}});
    const ww=(status.flows||[]).find(f=>f.name==='Warm water optimalisatie - PV boiler + CV advies v1.3')||{};
    const tes=(status.flows||[]).find(f=>/^Tesla laden v2\./.test(f.name)&&f.enabled&&!f.broken)||{};
    document.querySelectorAll('.ha-stage.control .ha-row').forEach(row=>{const spans=row.querySelectorAll(':scope > span');if(spans.length<2)return;const key=spans[0].textContent.trim();if(key==='Boiler')spans[1].innerHTML=ww.enabled?'via warmwater · actief <span class="dot ok"></span>':'regeling uit <span class="dot off"></span>';if(key==='Tesla / Easee')spans[1].innerHTML=tes.enabled?`${tes.name} · deadline + M7 · actief <span class="dot ok"></span>`:'regeling uit <span class="dot off"></span>';});
    const samples=phase.samples||[],latest=samples.length?samples[samples.length-1]:{};
    const runtime=runtimeData.status||latest.evDeadlineStatus||latest.EVDeadlineStatus||'';
    const latestStart=runtimeData.latestStart||latest.evLatestStart||latest.EVLatestStart||'';
    const active=command.active===true,currentSoc=Number(command.currentSoc),targetSoc=Number(command.targetSoc),hasSoc=Number.isFinite(currentSoc)&&Number.isFinite(targetSoc)&&targetSoc>currentSoc;
    const goals=[...document.querySelectorAll('.ha-stage.decision .ha-goal')],chargeGoal=goals.find(g=>g.querySelector('strong')?.textContent.trim()==='Tesla laden'),deadlineGoal=goals.find(g=>/^Tesla gereed|^Tesla deadline gemist$|^Tesla$/.test(g.querySelector('strong')?.textContent.trim()||''));
    const expired=active?expiredResult(command,runtimeData):null;
    if(active&&expired){
      if(deadlineGoal){const title=deadlineGoal.querySelector('strong'),state=deadlineGoal.querySelector('.ha-goal-state'),sub=deadlineGoal.querySelector('small');if(title)title.textContent=expired.headline;if(state){state.className=`ha-goal-state ${expired.kind==='reached'?'ok':'warn'}`;state.textContent=expired.state;}if(sub)sub.textContent=expired.sub;}
      if(chargeGoal){const state=chargeGoal.querySelector('.ha-goal-state'),sub=chargeGoal.querySelector('small'),rl=runtimeLabel(runtime);if(state&&rl){state.textContent=rl;state.className=`ha-goal-state ${expired.kind==='reached'?'ok':'warn'}`;}if(sub)sub.textContent=expired.kind==='reached'?'Deadline afgerond; nieuwe deadline kan worden ingesteld':expired.kind==='missed'?'Deadline verstreken; regeling kan resterende laadbehoefte apart afhandelen':'Deadline verstreken; controleer meetstatus voordat het resultaat als gehaald wordt beschouwd';}
    }else if(active){
      if(deadlineGoal){const title=deadlineGoal.querySelector('strong'),state=deadlineGoal.querySelector('.ha-goal-state'),sub=deadlineGoal.querySelector('small');if(title)title.textContent='Tesla gereed vóór';if(state){state.className='ha-goal-state warn';state.textContent=`Deadline actief · ${fmtDeadline(command.deadline)}`;}if(sub){const bits=[hasSoc?`${currentSoc}% → ${targetSoc}%`:'SOC opnieuw invoeren',`${Number(command.maxA||11)} A max`];if(latestStart)bits.push(`uiterlijk starten ${fmtDeadline(latestStart)}`);sub.textContent=bits.join(' · ');}}
      if(chargeGoal){const state=chargeGoal.querySelector('.ha-goal-state'),sub=chargeGoal.querySelector('small'),rl=runtimeLabel(runtime);if(state&&rl){state.textContent=rl;state.className=`ha-goal-state ${/catch-up|gehaald|laden/i.test(rl)&&!/fout|afwijking|ontbreekt|niet haalbaar|onder druk/i.test(rl)?'ok':'warn'}`;}if(sub)sub.textContent=rl?'M7 prijs/PV optimaliseert zolang de harde deadline ruimte laat':'Deadline is ingesteld; Homey-status wordt verwerkt';}
    }else if(deadlineGoal){const title=deadlineGoal.querySelector('strong'),state=deadlineGoal.querySelector('.ha-goal-state'),sub=deadlineGoal.querySelector('small');if(title)title.textContent='Tesla gereed vóór';if(state){state.className='ha-goal-state off';state.textContent='Geen deadline ingesteld';}if(sub)sub.textContent='Opportunistisch laden / exportbuffer';}
    const foot=document.querySelector('.ha-foot');if(foot){if(expired)foot.textContent=expired.kind==='reached'?'De vorige Tesla-deadline is afgerond. Stel alleen een nieuwe deadline in wanneer er een nieuwe laadverplichting is.':expired.kind==='missed'?'De Tesla-deadline is verstreken terwijl nog laadbehoefte resteerde. De oude deadline wordt niet meer als actief toekomstdoel gepresenteerd.':'De Tesla-deadline is verstreken, maar de meetstatus is onvoldoende betrouwbaar om gehaald/gemist vast te stellen.';else foot.textContent=active?(hasSoc?`Deadline ${currentSoc}% → ${targetSoc}% vóór ${fmtDeadline(command.deadline)}. De regeling benut actuele PV-export en M7 prijs/PV-forecast vóór het uiterste startmoment; vanaf dat moment is catch-up hard leidend.`:`Deze actieve deadline is nog met het oude kWh-formaat ingesteld. Bij de volgende wijziging voer je huidige SOC en doel-SOC in.`):'Zonder deadline wordt de Tesla alleen opportunistisch geladen en als flexibele exportbuffer ingezet; lage prijzen alleen veroorzaken dan geen netladen.';}
  }
  async function enhance(){await refreshData();applyOverlay();}
  function scheduleEnhance(){clearTimeout(enhanceTimer);enhanceTimer=setTimeout(()=>{enhance().catch(()=>{});},80);}
  script.onload=()=>{document.dispatchEvent(new Event('DOMContentSwitch'));scheduleEnhance();const root=document.getElementById('home-architecture');if(root)new MutationObserver(()=>scheduleEnhance()).observe(root,{childList:true,subtree:true});setInterval(()=>enhance().catch(()=>{}),30000);};
  script.onerror=()=>{const root=document.getElementById('home-architecture');if(root)root.innerHTML='<div class="ha-shell"><div class="ha-health"><strong>Architectuuroverzicht kon niet worden geladen</strong><span>Basisrenderer ontbreekt.</span></div></div>';};
  document.head.appendChild(script);
})();
