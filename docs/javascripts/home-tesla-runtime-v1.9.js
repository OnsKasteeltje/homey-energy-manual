(function(){
  const RUNTIME='/homey-energy-manual/data/tesla-runtime.json',STATUS='/homey-energy-manual/data/homey-status.json';
  const LABELS={GEEN_DEADLINE:'Opportunistisch',WACHT_OP_PV:'Wacht op PV',OPPORTUNISTISCH:'Opportunistisch laden',DEADLINE_WACHT:'Wacht op geschikt laadmoment',DEADLINE_CATCH_UP:'Catch-up actief',DEADLINE_GEMIST_CATCH_UP:'Deadline gepasseerd · catch-up',DEADLINE_PRIJS_NEGATIEF:'Laden · negatieve stroomprijs',DEADLINE_PV_OVERSCHOT:'Laden op PV-overschot',DEADLINE_PV_FORECAST:'Laden · gunstig PV-forecastuur',DEADLINE_PRIJS_GOEDKOOP:'Laden · gunstige stroomprijs',DEADLINE_WACHT_GOEDKOPER:'Wacht op goedkoper laadmoment',DOEL_GEHAALD:'Laaddoel gehaald',NIET_AANGESLOTEN:'Tesla niet aangesloten',CONFIG_FOUT:'Deadlineconfiguratie fout'};
  async function json(url){try{const r=await fetch(`${url}?ts=${Date.now()}`,{cache:'no-store'});return r.ok?await r.json():null;}catch(e){return null;}}
  async function apply(){const [d,s]=await Promise.all([json(RUNTIME),json(STATUS)]);if(!d&&!s)return;
    if(s){const tes=(s.flows||[]).find(f=>/^Tesla laden v2\./.test(f.name)&&f.enabled&&!f.broken)||(s.flows||[]).find(f=>f.category==='Tesla'&&f.enabled&&!f.broken);document.querySelectorAll('.ha-stage.control .ha-row').forEach(row=>{const spans=row.querySelectorAll(':scope > span');if(spans.length<2||spans[0].textContent.trim()!=='Tesla / Easee')return;spans[1].innerHTML=tes?`${tes.name} · actief <span class="dot ok"></span>`:'regeling uit <span class="dot off"></span>';});}
    if(d){const goals=[...document.querySelectorAll('.ha-stage.decision .ha-goal')],g=goals.find(x=>x.querySelector('strong')?.textContent.trim()==='Tesla laden');if(!g)return;const state=g.querySelector('.ha-goal-state'),sub=g.querySelector('small'),label=LABELS[String(d.status||'').toUpperCase()]||d.status||'';if(state&&label){state.textContent=label;state.className=`ha-goal-state ${/laden|catch-up|gehaald/i.test(label)?'ok':'warn'}`;}if(sub){const m=d.m7||{},bits=[];if(m.negative)bits.push('negatieve prijs');if(m.cheapNext4h)bits.push('prijs nu goedkoop t.o.v. 4u');if(m.expensiveNext4h)bits.push('prijs nu duur t.o.v. 4u');if(m.pvTop4h)bits.push('huidig uur top-4 PV');sub.textContent=`Tesla laden v2.3 · ${Number(d.targetA||0)} A gevraagd${bits.length?' · M7: '+bits.join(', '):''}`;}}
  }
  function start(){apply();setInterval(apply,20000);}document.addEventListener('DOMContentLoaded',start);document.addEventListener('DOMContentSwitch',apply);
})();
