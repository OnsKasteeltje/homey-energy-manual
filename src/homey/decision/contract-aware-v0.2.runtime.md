# EM v2 | 40 Decision | Contract-aware v0.2

- Homey flow ID: `11a78b36-a8a5-4988-915c-53b54351737e`
- Type: Advanced Flow
- Runtime state at capture: `enabled=false`, `broken=false`, `triggerable=true`
- Trigger: every 5 minutes + manual start
- Capture date: 2026-08-28
- Status: exact runtime baseline captured; not yet promoted/refactored

## Runtime HomeyScript

```js
const vs=await Homey.logic.getVariables(),b=Object.fromEntries(Object.values(vs).map(v=>[v.name,v])); const parse=v=>{try{return JSON.parse(String(v??'null'));}catch{return null;}},bool=v=>v===true||String(v).toLowerCase()==='true',num=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d; const set=async(n,t,val)=>{const v=b[n];if(v){await Homey.logic.updateVariable({id:v.id,variable:{value:val}});v.value=val;}else b[n]=await Homey.logic.createVariable({variable:{name:n,type:t,value:val}});}; const state=parse(b.EM2_State?.value),ww=parse(b.EM2_WW_State?.value),pc=parse(b.EM2_ContractPrice_Context?.value); if(!state||!pc){await set('EM2_ContractDecision_Status','string',JSON.stringify({status:'BLOCKED_CONTEXT_MISSING',at:new Date().toISOString(),hasState:!!state,hasPriceContext:!!pc}));return true;} const age=Date.now()-Date.parse(String(b.EM2_ContractPrice_UpdatedAt?.value||pc.updatedAt||'')),priceFresh=Number.isFinite(age)&&age>=0&&age<=35*60000,quality=String(b.EM2_ContractPrice_Quality?.value||pc.quality||'MISSING'),contract=String(pc.contractType||b.EM2_Contract_Type?.value||'DYNAMIC').toUpperCase(),neg=priceFresh&&quality==='GOOD'&&bool(b.EM2_ContractPrice_Negative?.value),cheap=priceFresh&&quality==='GOOD'&&bool(b.EM2_ContractPrice_Cheap_Next4h?.value),expensive=priceFresh&&quality==='GOOD'&&bool(b.EM2_ContractPrice_Expensive_Next4h?.value),horizon=String(b.EM2_ContractPrice_Horizon?.value||pc.horizon||''),priceUsable=priceFresh&&quality==='GOOD'&&(contract==='FIXED'||horizon==='FULL'||horizon==='INTRADAY'||horizon==='STATIC'); const gridW=num(state.grid?.powerW),exportW=Math.max(0,-gridW),importW=Math.max(0,gridW),flex=Math.max(0,num(state.energyBudget?.flexExportBudgetW)),disc=Math.max(0,num(state.energyBudget?.discretionaryImportBudgetW)),gridMeasurementValid=state.energyBudget?.gridMeasurementValid===true||state.balance?.controlGate?.gridMeasurementValid===true,derivedHouseBalanceValid=state.energyBudget?.derivedHouseBalanceValid===true||state.balance?.controlGate?.derivedHouseBalanceValid===true,balanceValid=derivedHouseBalanceValid,quattW=Math.max(0,num(state.quatt?.powerW)),quattReserve=Math.max(0,num(state.energyBudget?.quattRampReserveW)),chargeState=String(state.tesla?.chargeState||'unknown').toLowerCase(),plugged=!chargeState.includes('plugged_out')&&!chargeState.includes('unplug'),deadline=bool(state.goals?.teslaDeadlineActive),remaining=Math.max(0,num(state.goals?.teslaRemainingKWh)),latest=Date.parse(String(state.goals?.teslaLatestStart||'')),priceBudgetOk=disc>=1500; let priority='MAY',intent='HOLD',reason='Geen harde verplichting of sterke opportunity'; if(deadline&&remaining>0&&Number.isFinite(latest)&&Date.now()>=latest){priority='MUST';intent=plugged?'TESLA_CHARGE_DEADLINE':'TESLA_DEADLINE_BLOCKED_NOT_CONNECTED';reason=`Tesla deadline catch-up: ${remaining.toFixed(2)} kWh resterend`;} else if(deadline&&remaining>0&&(flex>=800||(priceUsable&&neg)||(priceUsable&&cheap&&priceBudgetOk))){priority='SHOULD';intent=plugged?'TESLA_CHARGE_OPPORTUNITY':'TESLA_WAIT_NOT_CONNECTED';reason=flex>=800?`Flex-exportbudget ${Math.round(flex)} W`:neg?'Negatieve prijs':`${contract} goedkoop prijsvenster; importbudget ${Math.round(disc)} W`;} else if(!deadline&&plugged&&flex>=1500){priority='MAY';intent='TESLA_BUFFER_EXPORT';reason=`Geen deadline; flex-exportbudget ${Math.round(flex)} W`;}
const ts=new Date().toISOString(),decision={schema:'EM2_CONTRACT_DECISION_V0.2',ts,readOnly:true,controlMode:'SHADOW_CANDIDATE',sourceRevision:Number(state.revision)||0,contract,priceSource:pc.source,priceQuality:quality,priceFresh,priceUsable,priority,intent,reason,inputs:{gridW,exportW,importW,flexExportBudgetW:flex,discretionaryImportBudgetW:disc,gridMeasurementValid,derivedHouseBalanceValid,balanceValid,quattPowerW:quattW,quattRampReserveW:quattReserve,deadlineActive:deadline,remainingKWh:remaining,latestStart:state.goals?.teslaLatestStart??null,priceNegative:neg,priceCheapNext4h:cheap,priceExpensiveNext4h:expensive,importPriceNow:num(b.EM2_ContractPrice_Import_Now?.value,-99),exportPriceNow:num(b.EM2_ContractPrice_Export_Now?.value,-99),selfUseGain:num(b.EM2_ContractPrice_SelfUse_Gain?.value,-99)},safety:{noActuatorWrites:true,legacyM7PriceInputs:false,candidateNamespaceIsolated:true,p1AuthoritativeForFlex:true,derivedBalanceDiagnosticOnly:true}}; await set('EM2_Decision_ContractCandidate','string',JSON.stringify(decision));
const mode=state.hotWater?.mode===true,boilerOn=state.hotWater?.boilerOn===true,goal=ww?.goalReachedToday===true||ww?.goalReached===true,remainingMin=Math.max(0,num(ww?.remainingFallbackMin,240)),catchup=ww?.catchupRequired===true,pvTop=state.context?.fresh===true&&bool(state.context?.pvTop4h),strongExport=gridMeasurementValid&&flex>=1900,pvOpportunity=gridMeasurementValid&&pvTop&&flex>=500,priceImportGuardOk=importW+1900<=4000,priceOpportunity=priceUsable&&(neg||(cheap&&priceImportGuardOk)),parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Berlin',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date()),hh=Number(parts.find(x=>x.type==='hour')?.value||0),mm=Number(parts.find(x=>x.type==='minute')?.value||0),minuteOfDay=hh*60+mm; let action='HOLD',wp='MAY',wr='Geen wijziging nodig',opp='WAIT'; if(!mode){action=boilerOn?'BOILER_OFF':'HOLD';wp='MUST';wr='Elektrische boilermodus is niet geselecteerd';opp='BLOCKED_MODE';} else if(goal){action=boilerOn?'BOILER_OFF':'HOLD';wp='MUST';wr='Dagdoel bereikt';opp='GOAL_REACHED';} else if(minuteOfDay>=1140){action=boilerOn?'BOILER_OFF':'HOLD';wp='MUST';wr='Na 19:00 geen elektrische warmwater-run';opp='AFTER_DEADLINE';} else if(catchup){action=boilerOn?'HOLD':'BOILER_ON';wp='MUST';wr=`Catch-up vereist: ${Math.round(remainingMin)} min resterend`;opp='CATCHUP';} else if(minuteOfDay<570){action=boilerOn?'BOILER_OFF':'HOLD';wp=boilerOn?'SHOULD':'MAY';wr='Voor 09:30 wachten op PV/prijs-opportunity';opp='WAIT_MORNING';} else if(!boilerOn&&strongExport){action='BOILER_ON';wp='SHOULD';wr=`P1-authoritatief flex-exportbudget ${Math.round(flex)} W`;opp='EXPORT';} else if(!boilerOn&&priceUsable&&neg){action='BOILER_ON';wp='SHOULD';wr='Negatieve prijs uit uniforme prijscontext';opp='PRICE_NEGATIVE';} else if(!boilerOn&&priceUsable&&cheap&&priceImportGuardOk){action='BOILER_ON';wp='SHOULD';wr=`${contract} goedkoop prijsvenster; geprojecteerde import ${Math.round(importW+1900)} W`;opp='PRICE_CHEAP';} else if(!boilerOn&&priceUsable&&cheap&&!priceImportGuardOk){wr=`Prijs gunstig maar onvoldoende importbudget: ${Math.round(disc)} W`;opp='WAIT_IMPORT_BUDGET';} else if(!boilerOn&&pvOpportunity){action='BOILER_ON';wp='SHOULD';wr=`Top-4 PV-forecastuur met P1-authoritatief ${Math.round(flex)} W flex-exportbudget`;opp='PV_FORECAST';} else if(boilerOn&&!priceOpportunity&&(expensive||importW>500)){action='BOILER_OFF';wp='SHOULD';wr=expensive?'Prijscontext ongunstig':'Geen opportunity en netto import';opp=expensive?'WAIT_PRICE':'WAIT_IMPORT';} else {wr=!gridMeasurementValid?'P1/netmeting ongeldig of stale; export/PV-opportunity fail-closed':!derivedHouseBalanceValid?'Afgeleide PV/huisbalans ongeldig; diagnostiek degraded maar P1-flex blijft geldig':priceUsable?'Wachten op opportunity':'Prijscontext niet bruikbaar; comfort/PV/catch-up fallback';opp=!gridMeasurementValid?'WAIT_P1':priceUsable?'WAIT_OPPORTUNITY':'WAIT_PRICE_CONTEXT';} const wwc={schema:'EM2_CONTRACT_WW_CONTROL_V0.2',generatedAt:ts,controlMode:'SHADOW_CANDIDATE',readOnly:true,contract,action,priority:wp,reason:wr,opportunity:opp,guards:{priceFresh,priceUsable,priceQuality:quality,gridMeasurementValid,derivedHouseBalanceValid,balanceValid,noActuatorWrites:true,legacyM7PriceInputs:false,candidateNamespaceIsolated:true,p1AuthoritativeForFlex:true,derivedBalanceDiagnosticOnly:true},inputs:{boilerOn,mode,goalReachedToday:goal,remainingFallbackMin:remainingMin,catchupRequired:catchup,gridW,importW,exportW,flexExportBudgetW:flex,pvTop4h:pvTop,priceNegative:neg,priceCheapNext4h:cheap,priceExpensiveNext4h:expensive,importPriceNow:num(b.EM2_ContractPrice_Import_Now?.value,-99),exportPriceNow:num(b.EM2_ContractPrice_Export_Now?.value,-99),selfUseGain:num(b.EM2_ContractPrice_SelfUse_Gain?.value,-99)}}; await set('EM2_Control_WW_ContractCandidate','string',JSON.stringify(wwc)); await set('EM2_ContractDecision_Status','string',JSON.stringify({status:'OK',at:ts,version:'v0.2',contract,priceSource:pc.source,priceQuality:quality,priceFresh,priceUsable,sourceRevision:Number(state.revision)||0,gridMeasurementValid,derivedHouseBalanceValid})); return true;
```

## Runtime note

`v0.2 split-validity: fresh P1/netmeting is authoritative for flex/export opportunities. Derived PV/house balance is diagnostic only and may degrade under SOURCE_SKEW without blocking P1 flex. Shadow-only; no actuator writes.`
