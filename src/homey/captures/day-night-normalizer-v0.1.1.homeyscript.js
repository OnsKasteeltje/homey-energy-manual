// Runtime capture 2026-08-30
// Flow ID: a41079f7-2287-4ec0-9e9b-27619e93ba35
// Homey runtime enabled=false, broken=false; every 5 min + 30 s delay; day/night branch.

// DAY branch
async function dayBranch(){
const vars=await Homey.logic.getVariables();const by=Object.fromEntries(Object.values(vars).map(v=>[v.name,v]));const parse=x=>{try{return JSON.parse(String(x??''));}catch{return null;}};const set=async(n,t,val)=>{const v=by[n];if(v){if(v.value!==val){await Homey.logic.updateVariable({id:v.id,variable:{value:val}});v.value=val;}return;}by[n]=await Homey.logic.createVariable({variable:{name:n,type:t,value:val}});};
const now=new Date().toISOString(),s=parse(by.EM2_State?.value),p=parse(by.EM2_Public_State?.value);if(!s?.sourceTiming){await set('EM2_Freshness_Status','string','BLOCKED_CORE_STATE_MISSING');await set('EM2_Freshness_Alarm','boolean',true);await set('EM2_Freshness_UpdatedAt','string',now);return true;}
const st=s.sourceTiming,src=st.freshness||{},p1Fresh=st.p1Fresh===true,pvBad=['solarEdge','goodWe4200','goodWe2000'].filter(k=>src[k]?.fresh!==true);let status='OK',alarm=false,reason='Alle vereiste bronnen vers';if(!p1Fresh){status='UNEXPECTED_STALE_P1';alarm=true;reason='P1 is niet vers; control blijft fail-closed';}else if(pvBad.length){status='UNEXPECTED_STALE_DAY';alarm=true;reason=`PV-bron(nen) overdag stale: ${pvBad.join(', ')}`;}const raw=s.balance?.rawReason||s.balance?.reason||null,normalized=(p1Fresh&&raw==='SOURCE_STALE_OR_MISSING'&&pvBad.length)?status:raw;
s.freshnessPolicy={schema:'EM2_FRESHNESS_V0.1',evaluatedAt:now,period:'DAY',status,alarm,p1Fresh,stalePvSources:pvBad,rawBalanceReason:raw,normalizedBalanceReason:normalized,policy:{p1:'ALWAYS_REQUIRED_FAIL_CLOSED',pvDay:'REQUIRED_FOR_RECONSTRUCTION_AND_ALERT',pvNight:'EXPECTED_STALE_IF_P1_FRESH'}};if(s.balance){s.balance.rawReason=raw;s.balance.reason=normalized;s.balance.freshnessStatus=status;}if(s.energyBudget){s.energyBudget.rawBalanceReason=s.energyBudget.rawBalanceReason||s.energyBudget.balanceReason;s.energyBudget.balanceReason=normalized;}await set('EM2_State','string',JSON.stringify(s));
if(p?.balance){const pr=p.balance.raw_reason||p.balance.reason||null,pn=(p1Fresh&&pr==='SOURCE_STALE_OR_MISSING'&&pvBad.length)?status:pr;p.balance.raw_reason=pr;p.balance.reason=pn;p.balance.freshness_status=status;p.balance.freshness_expected=false;if(p.energy_budget){p.energy_budget.raw_balance_reason=p.energy_budget.raw_balance_reason||p.energy_budget.balance_reason;p.energy_budget.balance_reason=pn;}p.meta=p.meta||{};p.meta.freshness_policy='EM2_FRESHNESS_V0.1';p.meta.freshness_evaluated_at=now;await set('EM2_Public_State','string',JSON.stringify(p));await set('EM2_Publish_Due','boolean',true);}
const detail={schema:'EM2_FRESHNESS_V0.1',evaluatedAt:now,period:'DAY',status,alarm,reason,p1Fresh,stalePvSources:pvBad,rawBalanceReason:raw,normalizedBalanceReason:normalized,selfTest:{freshAll:'OK',nightPvStale:'EXPECTED_STALE_NIGHT',dayPvStale:'UNEXPECTED_STALE_DAY',p1Stale:'UNEXPECTED_STALE_P1'}};await set('EM2_Freshness_Status','string',status);await set('EM2_Freshness_Alarm','boolean',alarm);await set('EM2_Freshness_Detail','string',JSON.stringify(detail));await set('EM2_Freshness_UpdatedAt','string',now);return true;
}

// NIGHT branch
async function nightBranch(){
const vars=await Homey.logic.getVariables();const by=Object.fromEntries(Object.values(vars).map(v=>[v.name,v]));const parse=x=>{try{return JSON.parse(String(x??''));}catch{return null;}};const set=async(n,t,val)=>{const v=by[n];if(v){if(v.value!==val){await Homey.logic.updateVariable({id:v.id,variable:{value:val}});v.value=val;}return;}by[n]=await Homey.logic.createVariable({variable:{name:n,type:t,value:val}});};
const now=new Date().toISOString(),s=parse(by.EM2_State?.value),p=parse(by.EM2_Public_State?.value);if(!s?.sourceTiming){await set('EM2_Freshness_Status','string','BLOCKED_CORE_STATE_MISSING');await set('EM2_Freshness_Alarm','boolean',true);await set('EM2_Freshness_UpdatedAt','string',now);return true;}
const st=s.sourceTiming,src=st.freshness||{},p1Fresh=st.p1Fresh===true,pvBad=['solarEdge','goodWe4200','goodWe2000'].filter(k=>src[k]?.fresh!==true);let status='OK',alarm=false,reason='Alle vereiste bronnen vers';if(!p1Fresh){status='UNEXPECTED_STALE_P1';alarm=true;reason='P1 is niet vers; control blijft fail-closed';}else if(pvBad.length){status='EXPECTED_STALE_NIGHT';alarm=false;reason=`PV-bron(nen) 's nachts stale zoals verwacht: ${pvBad.join(', ')}`;}const raw=s.balance?.rawReason||s.balance?.reason||null,normalized=(p1Fresh&&raw==='SOURCE_STALE_OR_MISSING'&&pvBad.length)?status:raw;
s.freshnessPolicy={schema:'EM2_FRESHNESS_V0.1',evaluatedAt:now,period:'NIGHT',status,alarm,p1Fresh,stalePvSources:pvBad,rawBalanceReason:raw,normalizedBalanceReason:normalized,policy:{p1:'ALWAYS_REQUIRED_FAIL_CLOSED',pvDay:'REQUIRED_FOR_RECONSTRUCTION_AND_ALERT',pvNight:'EXPECTED_STALE_IF_P1_FRESH'}};if(s.balance){s.balance.rawReason=raw;s.balance.reason=normalized;s.balance.freshnessStatus=status;}if(s.energyBudget){s.energyBudget.rawBalanceReason=s.energyBudget.rawBalanceReason||s.energyBudget.balanceReason;s.energyBudget.balanceReason=normalized;}await set('EM2_State','string',JSON.stringify(s));
if(p?.balance){const pr=p.balance.raw_reason||p.balance.reason||null,pn=(p1Fresh&&pr==='SOURCE_STALE_OR_MISSING'&&pvBad.length)?status:pr;p.balance.raw_reason=pr;p.balance.reason=pn;p.balance.freshness_status=status;p.balance.freshness_expected=status==='EXPECTED_STALE_NIGHT';if(p.energy_budget){p.energy_budget.raw_balance_reason=p.energy_budget.raw_balance_reason||p.energy_budget.balance_reason;p.energy_budget.balance_reason=pn;}p.meta=p.meta||{};p.meta.freshness_policy='EM2_FRESHNESS_V0.1';p.meta.freshness_evaluated_at=now;await set('EM2_Public_State','string',JSON.stringify(p));await set('EM2_Publish_Due','boolean',true);}
const detail={schema:'EM2_FRESHNESS_V0.1',evaluatedAt:now,period:'NIGHT',status,alarm,reason,p1Fresh,stalePvSources:pvBad,rawBalanceReason:raw,normalizedBalanceReason:normalized,selfTest:{freshAll:'OK',nightPvStale:'EXPECTED_STALE_NIGHT',dayPvStale:'UNEXPECTED_STALE_DAY',p1Stale:'UNEXPECTED_STALE_P1'}};await set('EM2_Freshness_Status','string',status);await set('EM2_Freshness_Alarm','boolean',alarm);await set('EM2_Freshness_Detail','string',JSON.stringify(detail));await set('EM2_Freshness_UpdatedAt','string',now);return true;
}
