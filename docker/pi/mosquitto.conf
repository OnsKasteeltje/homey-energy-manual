persistence true
persistence_location /mosquitto/data/
listener 1883
allow_anonymous true

# Bootstrap LAN-internal transport only. Before exposure beyond the trusted EMS
# network, replace anonymous access with authenticated credentials/TLS.
